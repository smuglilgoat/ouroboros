# Session 2026-09-15 — wiki-ingest implementation

## Goal
Build the planned capture → ingest pipeline: MV3 Chrome extension (YouTube
transcript + page capture → companion or GitHub fallback) and a local Node
companion running `opencode` ingest with git commit/push.

## Branch
`agent/wiki-ingest-app` (off `agent/wiki-ingest-plan`).

## Files created/changed
- `extension/manifest.json`, `extension/background.js`, `extension/lib/parse.mjs`
- `extension/popup.html|js`, `extension/options.html|js`
- `companion/companion.mjs`, `companion/companion.config.example.json`
- `test/parse.test.mjs`, `test/companion.test.mjs`
- `README.md` (usage)

## Commands run
- `node test/parse.test.mjs` ✓, `node test/companion.test.mjs` ✓ (ran twice, stable)

## Notes / findings
- Test bug found: companion writes to `<wikiDir>/inbox/`, test initially checked repo root.
- Git strips embedded double quotes from `-m` messages on this setup; relaxed assertion.
- Companion's `git pull`/`push` failures are caught + logged, ingest still succeeds locally.
- End-to-end with real YouTube video + real wiki repo NOT yet verified (needs
  user's PAT, wiki clone, opencode installed) — listed as follow-up.
